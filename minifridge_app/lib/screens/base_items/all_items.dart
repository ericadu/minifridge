import 'package:flutter/material.dart';
import 'package:minifridge_app/models/base_item.dart';

class AllItems extends StatelessWidget {
  final List<BaseItem> food;

  AllItems({
    this.food
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}